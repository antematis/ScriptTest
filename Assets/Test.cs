using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //変数を初期化
        int a = 3;
        int b = 4;
        //変数aと変数bの加算した値を変数cに格納
        int c = a + b;
        //変数cの値を表示する
        Debug.Log(c);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
